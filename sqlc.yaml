version: "2"
sql:
  - engine: "postgresql"
    schema: "hub/migrations"
    queries: "hub/queries"
    gen:
      go:
        package: "db"
        out: "hub/db"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_pointers_for_null_types: true